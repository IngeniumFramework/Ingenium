{
  "name": "ingenium-redis",
  "version": "0.0.1",
  "description": "Redis-backed session, idempotency, and rate-limit stores for Ingenium. Drop-in replacements for the in-memory defaults; required for multi-instance deployments.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "redis",
    "session",
    "idempotency",
    "rate-limit",
    "ingenium",
    "multi-instance"
  ],
  "license": "MIT",
  "peerDependencies": {
    "ingenium": "*"
  },
  "peerDependenciesMeta": {
    "ingenium": { "optional": false }
  },
  "devDependencies": {
    "vitest": "^4.1.7"
  }
}
